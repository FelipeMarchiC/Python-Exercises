def check_diff_characters(string):
  for i in range(len(string)):
    for j in range(i + 1, len(string)):
      if string[i] == string[j]:
        return False
  return True

string = input()
all_characters_are_different = check_diff_characters(string)
print(all_characters_are_different)
